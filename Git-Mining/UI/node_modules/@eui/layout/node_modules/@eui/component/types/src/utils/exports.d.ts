/**
 * @license
 * COPYRIGHT Ericsson 2022
 * The copyright to the computer program(s) herein is the property of
 * Ericsson Inc. The programs may be used and/or copied only with written
 * permission from Ericsson Inc. or in accordance with the terms and
 * conditions stipulated in the agreement/contract under which the
 * program(s) have been supplied.
 */
/**
 * A decorator used to decorate props in a component.
 * use like...
 *
 * @prop({
 *   attribute: [true|false],
 *   type: ['string'|'number'|'boolean'|'array'|'object'],
 *   default: value, required: [true|false]})
 * propA;
 *
 * @param {Object} options - define the prop.
 * @deprecated
 */
export function prop(options?: any): (target: any, name: any, descriptor: any) => void;
export function registerMixin(superMixins: any, name: string): any[];
export function hasMixin(Base: any, name: string): boolean;
