export declare class Prefill {
  private dom: {
    prefill: HTMLInputElement;
    saved: string;
  };
  private listeners: {
    blur(evt: Event): void;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _setSavedValue(): void;

  private _checkIfChanged(evt: Event): void;
}
