import { Menu } from './Menu';

export declare class Dropdown {
  private dom: {
    dropdown: HTMLElement;
    button: HTMLElement;
    menu: HTMLElement;
    items: HTMLElement;
  };

  private type: 'click' | 'divided';

  private listeners: {
    toggleMenu: () => void;
    hideMenu: () => void;
    clickOutside: (evt: Event) => void;
  };

  private isDisabled: boolean;

  private highlightIndex: Number;

  private menu: Menu;

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _setDisabled(): void;

  private _hideIfClickedOutside(event: Event): void;

  private _arrowKeyActions(event: Event): void;

  private _highlightOptions(): void;

  private _removeOptionsHighlight(): void;
}
