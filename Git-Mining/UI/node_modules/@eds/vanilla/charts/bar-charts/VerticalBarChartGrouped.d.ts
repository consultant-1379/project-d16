interface Series {
  name: string;
  values: number[];
}

interface Data {
  common: string[],
  series: Series[]
}

interface Y {
  unit?: string;
}

interface Margin {
  left?: number;
  right?: number;
  top?: number;
  bottom?: number;
}

interface Thresholds {
  color: string;
  moreThan?: number;
  lessThan?: number;
}

interface Props {
  data: Data;
  element: HTMLElement;
  y?: Y;
  height?: number;
  margin?: Margin;
  thresholds?: Thresholds[];
}

export declare class VerticalBarChartGrouped {
  constructor(props: Props);

  init(): void;
  destroy(): void;
  draw(data?: Data): void;
  private _highlight(key: String): void;
}
