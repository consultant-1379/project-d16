interface Series {
  name: string;
  values: number[];
}

interface Data {
  common: string[],
  series: Series[]
}

interface X {
  unit?: string;
}

interface Margin {
  left?: number;
  right?: number;
  top?: number;
  bottom?: number;
}

interface Props {
  data: Data;
  element: HTMLElement;
  x?: X;
  height?: number;
  margin?: Margin;
}

export declare class HorizontalBarChartStacked {
  constructor(props: Props);

  init(): void;
  destroy(): void;
  draw(data?: Data): void;
  private _highlight(key: String): void;
}
