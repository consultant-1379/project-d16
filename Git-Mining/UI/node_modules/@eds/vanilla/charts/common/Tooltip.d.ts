interface Unit {
  unit?: {
    title?: string;
    value?: string;
  };
}

interface Format {
  format?: {
    title?: string;
    value?: string;
  };
}

interface Data {
  series: {
    name: string;
    values: number[];
  }[];
  common: number[] | string[];
}

interface Props {
  element: HTMLElement;
  data: Data;
  hoverClass: string;
  format?: Format;
  Unit?: Unit;
  isTime?: boolean;
  rawMode?: boolean;
  showSummary?: boolean; 
}

export declare class Tooltip {
  constructor(props: Props);

  init(): void;
  update(seriesIndex: number, newData: Data): void;
}
