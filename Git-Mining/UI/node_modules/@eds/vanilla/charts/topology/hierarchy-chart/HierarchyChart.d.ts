export interface Menu {
  title: string;
}

export interface Serie {
  name: string;
}

export interface Alert {
  name: string;
  value: string | number;
  descriptions?: string[];
}

export interface Node {
  open?: boolean;
  key: string;
  hasSibling?: boolean;
  title?: string;
  subtitle?: string;
  descriptions?: string[];
  menus?: Menu[];
  alerts?: Alert[];
  children?: Node[];
}

export interface Data {
  alerts: Serie[];
  nodes: Node[];
}

export interface Props {
  element: HTMLElement;
  data: Data;
  height?: number;
  horizontal?: boolean;
  alerts?: boolean;
  compact?: boolean;
  onDropdownClick?: (Menu) => void;
}

export declare class HierarchyChart {
  constructor(props: Props);

  init(): void;
  draw(): void;
  destroy(): void;
}
