interface Margin {
  left?: number;
  right?: number;
  top?: number;
  bottom?: number;
}

interface Range {
  unit?: string;
  reverse?: boolean;
}

interface Data {
  commonX: string[] | number[];
  commonY: string[] | number[];
  series: {
    name: string;
    values: {
      value: number;
    }[][];
  }[];
}

interface Tooltip {
  format?: {
    title?: string;
    label?: string;
    value?: string;
  };
  isHidden?: boolean;
}

interface Props {
  data: Data;
  element: HTMLElement;
  height?: number;
  margin?: Margin;
  range?: Range;
  tooltip?: Tooltip;
  y?: {
    tickFormat?: string;
    isTime?: boolean;
    unit?: string;
  };
  x?: {
    tickFormat?: string;
    isTime?: boolean;
    unit?: string;
  };
  palette?: string;
}

export declare class Heatmap {
  constructor(props: Props);
  init(): void;
  destroy(): void;
  draw(newData?: Data): void;
}