import { Input } from '@angular/core';

export declare class SignIn {
  private dom: {
    signin: HTMLElement;
    reveal: HTMLElement;
    inputPassword: HTMLInputElement;
    form: HTMLFormElement;
    error: HTMLElement;
    welcome: HTMLElement;
    loggedInUrl: string;
  };
  private listeners: {
    click: () => void;
    blur: (evt: Event) => void;
    submit: (evt: Event) => void;
    focus: (evt: Event) => void;
    inputsBlur: (evt: Event) => void;
  };
  private messages: {
    valueMissing: string;
    typeMissing: {
      email: string;
      fallback: string;
    };
    patternMismatch: {
      signum: string;
      fallback: string;
    };
    fallback: string;
  };
  private props: {
    validFields: boolean[];
  };

  constructor(element: HTMLElement, loggedInUrl?: string);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _revealPassword(): void;

  private _removeNativeValidation(): void;

  private _validateFieldOnBlur(evt: Event): void;

  private _validateFormOnSubmit(evt: Event): void;

  private _getErrorType(field: HTMLInputElement): string;

  private _successSignIn(): void;

  private _showWelcomeScreen(): void;

  private _hideSignInForm(): void;

  private _labelAnimation(evt: Event): void;

  private _checkEmptyField(evt: Event): void;
}
