import { Tile } from './Tile';

export declare class Page {
  private dom: {
    page: HTMLElement;
    tiles: NodeListOf<Element>;
  };

  private listeners: {
    maximize: (event: Event) => void;
    minimize: (event: Event) => void;
  };

  private tiles: Tile[];

  constructor();

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _showAll(event: CustomEvent): void;

  private _hideNotSelf(event: CustomEvent): void;

  private _scrollToTile(tileId: string): void;
}
