export declare class Tile {
  private dom: {
    tile: HTMLElement;
    header: HTMLElement;
    actions: NodeListOf<Element>;
    id: string;
  };
  private events: {
    maximizeTile: CustomEvent;
    minimizeTile: CustomEvent;
  };
  private listeners: {
    hideNotSelf: (event: CustomEvent) => void;
    show: () => void;
    toggleFullScreen: (() => void)[];
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _toggleFullScreen(element: HTMLElement): void;

  private _maximize(element: HTMLElement): void;

  private _minimize(element: HTMLElement): void;

  private _setTooltipMessage(element: HTMLElement, message: string): void;

  show(): void;

  hide(): void;
}
