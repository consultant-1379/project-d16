import { Calendar } from './Calendar';

export declare class Datepicker {
  private dom: {
    datepicker: HTMLElement,
    calendar: HTMLElement,
    button: HTMLElement,
    input: HTMLElement,
    days: NodeListOf<HTMLTableDataCellElement>,
    hiddenInput: HTMLElement,
  };
  private listeners: {
    clickOutside: (evt: Event) => void,
    clickDay: (evt: Event) => void,
    clickButton: () => void;
    clickInput: () => void;
    changeInput: () => void;
  };
  private calendar: Calendar;
  private selectedDate: {
    year: string,
    month: string,
    day: string,
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _isValidDate(dateString: string): boolean;

  private _handleDateChange(): void;

  private _correctDateFormat(): void;

  private _setSelectedDateCSS(): void;

  private _hideCalendar(): void;

  private _showCalendar(): void;

  private _toggleCalendar(): void;

  private _hideIfClickedOutside(evt: Event): void;

  private _selectDay(evt: Event): void;

  setMaximumDate(maxDate: String): void;

  setMinimumDate(minDate: String): void;
}
