export declare class Calendar {
  private dom: {
    calendar: HTMLElement;
    year: HTMLElement;
    month: HTMLElement;
    days: NodeListOf<Element>;
    prevYear: HTMLElement;
    prevMonth: HTMLElement;
    nextMonth: HTMLElement;
    nextYear: HTMLElement;
    body: HTMLElement;
  };

  private eng: {
    months: string[];
    days: string[];
  };

  private listeners: {
    clickPrevYear: () => void;
    clickPrevMonth: () => void;
    clickNextMonth: () => void;
    clickNextYear: () => void;
  };

  calendar: {
    year: string;
    month: string;
    day: string[];
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _setPrevYear(): void;

  private _setPrevMonth(): void;

  private _setNextMonth(): void;

  private _setNextYear(): void;

  private _createCalendarBody(): void;

  private _setDOMCalendar(): void;

  private _getDayNum(day: string): string;

  private _getMonthNum(month: string): string;

  setDataCalendar(date: Date): void;

  formatJsDate(date: Date): string;

  formatDate(date: { year: string, month: string, day: string }): string;

  removeAllSelectedDays(): void;

  setMaximumDate(maxDate: String): void;

  setMinimumDate(minDate: String): void;
}
