
interface NotificationProps {
  title: string;
  description?: string;
  icon?: string;
  action?: Function;
  timeout?: number;
}

export declare class Notification {
  private dom: {
    notification: HTMLElement | null;
    close: Element | null;
  };
  private listeners: {
    click: (evt: Event) => void;
    keyDown: (evt: Event) => void;
  };
  props: NotificationProps;

  constructor(props: NotificationProps);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _createNotification(): void;

  private _handleClick(evt: Event): void;

  private _closeOnKeyDown(evt: Event): void;

  getNotification(): HTMLElement;
}
