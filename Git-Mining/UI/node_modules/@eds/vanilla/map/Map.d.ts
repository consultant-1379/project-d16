import mapboxgl from 'mapbox-gl';

interface IgeoJSON {
  type: string;
  features: {
    type: string;
    geometry: {
      type: string;
      coordinates: number[][];
    };
  };
}

declare const geoJSONLines: IgeoJSON;

declare const geoJSON: IgeoJSON;

export declare class Map {
  private mapCenter: number[];
  private themes: {
    dark: string;
    light: string
  };
  private lineColors: {
    dark: string;
    light: string
  };
  private loadedTheme: DOMTokenList;
  private map: mapboxgl.Map;
  private listeners: {
    centerMap: () => void;
    switchTheme: (event: Event) => void;
  };
  private lines: {
    id: string;
    type: string;
    source: {
      type: string;
      data: IgeoJSON;
    };
    layout: {
      'line-join': string;
      'line-cap': string;
    };
    paint: {
      'line-color': string;
      'line-width': number;
      'line-dasharray': number[];
      'line-opacity': number;
    };
  };

  constructor();

  private addEventListeners(): void;

  private removeEventListeners(): void;

  private centerMap():  void;

  private switchTheme(event: Event): void;

  init(): void;

  destroy(): void;
}
