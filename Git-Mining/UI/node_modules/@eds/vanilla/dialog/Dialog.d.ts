export declare class Dialog {
  private dom: {
    dialog: HTMLElement;
    content: HTMLElement;
    id: string;
    closeButtons: HTMLElement[];
  };
  private type: string;
  private trigger: string;
  private events: {
    closeDialogEvent: CustomEvent;
    showDialogEvent: CustomEvent;
  };
  private listeners: {
    show: () => void;
    hide: () => void;
    triggerDialog: (evt: CustomEvent) => void;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  show(): void;

  hide(): void;

  private _getCloseButtons(): void;

  private _triggerDialog(evt: CustomEvent): void;
}
