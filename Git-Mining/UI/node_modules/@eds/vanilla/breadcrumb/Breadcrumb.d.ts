export declare class Data {
  title: string;
  action?: () => void;
}

export declare class Breadcrumb {
  private dom: {
    breadcrumb: HTMLElement;
    links: NodeListOf<Element>;
    separators: NodeListOf<Element>;
    more: {
      element?: HTMLElement;
      icon?: HTMLElement;
      menu?: HTMLElement;
      items: HTMLElement[];
    }
  };

  private listeners: {
    iconClick(): void;
    windowClick(event: Event): void;
  };

  constructor(htmlElement: HTMLElement);

  init(breadcrumb: Data[]): void;

  destroy(): void;

  private _buildBreadcrumb(): void;

  private _buildShortBreadcrumb(): void;

  private _buildLongBreadcrumb(): void;

  private _createLinkElement(): HTMLElement;

  private _createSeparatorElement(): HTMLElement;

  private _createMoreElement(items: Data[]): HTMLElement;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

}
