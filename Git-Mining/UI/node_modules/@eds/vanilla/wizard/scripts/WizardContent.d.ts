export declare class WizardContent {
  private dom: {
    content: HTMLElement;
  };
  private listeners: {
    showCorrectContent: (evt: CustomEvent) => void;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private showCorrectContent(evt: CustomEvent): void;

  private showContent(content: HTMLElement): void;

  private hideContent(content: HTMLElement): void;
}
