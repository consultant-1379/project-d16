import { WizardStepState } from './WizardSteps';

export declare class WizardFooter {
  private dom: {
    wizardFooter: HTMLElement;
    nextAction: HTMLElement;
    prevAction: HTMLElement;
  };
  private listeners: {
    nextStep: () => void;
    prevStep: () => void;
    updateButtons: (evt: CustomEvent) => void;
  };
  private target: string;
  private wizardState: WizardStepState;
  private events: {
    wizardStateChange: CustomEvent;
    wizardShowNextSteps: CustomEvent;
    wizardShowPrevSteps: CustomEvent;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private nextStep(): void;

  private prevStep(): void;

  private updateButtons(evt: CustomEvent): void;
}
