export declare class TableSelectable {
  private dom: {
    table: HTMLElement;
    rows: NodeListOf<Element>;
    bodyRows: NodeListOf<Element>;
    masterCheckbox: HTMLInputElement;
  };
  private listeners: {
    rows: (() => void)[];
    clickBodyRow(row: HTMLElement): void;
    clickMasterCheckbox(): void;
  };
  private settings: {
    selectRowOnFirstTDClick: boolean
  };
  private events: {
    tableRowsSelected: CustomEvent;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _toggleRowCheckbox(row: HTMLElement): void;

  private _toggleAllCheckboxes(): void;

  private _setMasterCheckboxState(): void;

  private _getNumberOfSelectedRows(): number;

  private _getNumberOfRows(): number;

  private _selectRow(row: HTMLElement): void;

  private _unselectRow(row: HTMLElement): void;

  private _isSelectable(): boolean;
}
