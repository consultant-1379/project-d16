export declare class Syspanel {
  private dom: {
    syspanel: HTMLElement;
    syspanelCloseIcons: NodeListOf<Element>
    items: NodeListOf<Element>
  };
  private listeners: {
    toggleSyspanel(e: Event): void;
    pageClick(e: Event): void;
    closeIconClickHandler(): void;
  };
  private events: {
    SyspanelOpenEvent: CustomEvent;
    SyspanelCloseEvent: CustomEvent;
    SyspanelTabEvent: CustomEvent;
  };

  private addEventListeners(): void;

  private removeEventListerners(): void;

  private toggle(e: Event): void;

  private open(): void;

  private close(): void;

  private isHidden(element: HTMLElement): boolean;

  private hide(element: HTMLElement): void;

  private show(element: HTMLElement): void;

  private hideAllExcept(element: HTMLElement): void;

  private openEvent(): void;

  private closeEvent(): void;

  private tabEvent(): void;

  private pageClickHandler(e: Event): void;

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;
}
