export declare class Appbar {
  private dom: {
    appbar: HTMLElement;
    navItem: HTMLElement;
    navToggle: HTMLElement;
    menuAnchor: HTMLElement;
    title: HTMLElement;
  };
  private events: {
    toggleNavigation: CustomEvent;
    showNavigation: CustomEvent;
    hideNavigation: CustomEvent;
  };
  private listeners: {
    toggleNavigation: () => void;
    hideNavigation: () => void;
    showNavigation: () => void;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _toggle(): void;

  private _hide(): void;

  private _show(): void;
}
