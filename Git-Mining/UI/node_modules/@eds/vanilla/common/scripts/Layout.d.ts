import { App } from './App';
import { Sysbar } from './Sysbar';
import { Syspanel } from './Syspanel';


export declare class Layout {
  private dom: {
    body: HTMLElement;
    app: HTMLElement;
    syspanel: HTMLElement;
    sysbar: HTMLElement;
  };
  private app: App;
  private syspanel: Syspanel;
  private sysbar: Sysbar;
  private listeners: {
    resize: () => void;
  };
  private events: {
    showNavigation: CustomEvent;
    hideNavigation: CustomEvent;
    toggleNavigation: CustomEvent;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _setContentBehavior(): void;
}
