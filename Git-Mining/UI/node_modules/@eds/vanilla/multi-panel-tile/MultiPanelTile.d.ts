export declare class MultiPanelTile {
  private dom: {
    multiPanelTile: HTMLElement;
    leftPanel: HTMLElement;
    midPanel: HTMLElement;
    rightPanel: HTMLElement;
    leftPanelTriggers: NodeListOf<Element>;
    rightPanelTriggers: NodeListOf<Element>;
  };
  private listeners: {
    toggleLeftPanel: () => void;
    toggleRightPanel: () => void;
    updatePanelBehavior: () => void;
  };
  private flags: {
    panelLeft: boolean;
    panelRight: boolean;
  };

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  private _addEventListeners(): void;

  private _removeEventListeners(): void;

  private _updatePanelBehavior(): void;

  private _isPanelClosed(panel: HTMLElement): boolean;

  private _togglePanel(panel: HTMLElement): void;

  private _showPanel(panel: HTMLElement): void;

  private _hidePanel(panel: HTMLElement): void;

}
